# DB#02-用户角色权限表

## 任务目标

完善用户、角色、权限、用户角色、角色权限等 RBAC 数据库基础。

## 本次实现

- `V1__init_schema.sql` 已包含 RBAC 基础表。
- 新增 `V2__seed_rbac.sql`：
  - 初始化权限码。
  - 绑定五类角色权限。
  - 创建开发期默认管理员 `admin/admin123`。
  - 补充用户名、关联表索引。

## 验证方式

启动 PostgreSQL 后运行后端，Flyway 自动执行迁移。

