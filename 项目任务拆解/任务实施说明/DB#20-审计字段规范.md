# DB#20-审计字段规范

## 任务目标

统一核心表审计字段，便于后续追踪数据创建、更新和软删除。

## 本次实现

多数核心业务表包含以下字段：

```text
created_by
updated_by
created_at
updated_at
deleted
```

只追加型日志表保留：

```text
created_by
created_at
```

Java 侧新增 `AuditFields` 基类，后续实体可继承或参考该字段约定。

